export const loadLottie = (asset: string) => {
  // Mock: Load Lottie animation for ISL avatar
  // In production: Use lottie-web to load and play asset
  console.log(`Loading ISL animation: ${asset}`);
  // Placeholder: Assume #lottie-viewport is the container
  // const animation = lottie.loadAnimation({ container: document.getElementById('lottie-viewport'), animationData: asset });
};
